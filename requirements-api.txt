# Runtime-only dependencies for the FinFlow serving image.
# Deliberately excludes batch/dev tooling (pyspark, datasets, great-expectations,
# mlflow, faker, pytest, ruff, black) — those run in CI / preprocessing, never in
# the request hot path. Keeping the serving image lean = faster cold starts on
# Cloud Run and a smaller attack surface.
#
# NOTE: torch is installed separately in the Dockerfile from the CPU-only index
# (https://download.pytorch.org/whl/cpu) to avoid pulling ~2 GB of CUDA libraries
# we never use on Cloud Run's CPU runtime.

# --- API / serving ---
fastapi==0.110.0
uvicorn==0.27.1
pydantic==2.6.3
httpx==0.27.0

# --- analytical data layer ---
duckdb==0.10.1
pyarrow==14.0.2
pandas==2.1.4
numpy==1.26.4

# --- ML (deterministic tools) ---
transformers==4.38.2
scikit-learn==1.3.2
pyod==1.1.2
statsmodels==0.14.1
scipy==1.12.0
rapidfuzz==3.6.1

# --- agent / LLM orchestration ---
langchain==0.1.12
langchain-groq==0.1.3
langgraph==0.0.38
langchain-community==0.0.28

# --- utilities ---
python-dotenv==1.0.1
loguru==0.7.2
